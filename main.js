const quarterValue = (number) => {
    const quarter = number / 4
    return quarter
}

// Invoke the function and store the return value
const returned = quarterValue(26)

// Log the return value
console.log(returned)